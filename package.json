{
  "name": "lovable-clone",
  "version": "1.0.0",
  "description": "Static UI replica of Lovable.dev",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html public/* dist/ 2>/dev/null || cp index.html dist/ && cp -r public/* dist/",
    "preview": "npx serve dist"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  }
}
